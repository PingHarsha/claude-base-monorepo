-- V1__init.sql
-- Baseline migration. Real schema lands in subsequent migrations as entities are introduced.
-- Flyway requires at least one applied migration to seed flyway_schema_history.

SELECT 1;
